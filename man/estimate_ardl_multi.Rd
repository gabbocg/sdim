% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oos-helpers.R
\name{estimate_ardl_multi}
\alias{estimate_ardl_multi}
\title{Estimate ARDL(p1, p2) model}
\usage{
estimate_ardl_multi(y, z, h, p)
}
\arguments{
\item{y}{Numeric vector of the target variable.}

\item{z}{Numeric matrix of additional regressors (e.g., factor estimates).}

\item{h}{Positive integer; forecast horizon.}

\item{p}{Integer vector of length 2: \code{c(p1, p2)} where \code{p1} is
the number of AR lags and \code{p2} the number of \code{z} lags.}
}
\value{
Coefficient vector (intercept, AR lags, then z lags).
}
\description{
Fits an autoregressive distributed lag model for the horizon-\code{h}
target, with \code{p1} lags of \code{y} and \code{p2} lags of additional
regressors \code{z} (e.g., extracted factors).
}
\examples{
y <- rnorm(200)
z <- matrix(rnorm(200 * 3), 200, 3)
coefs <- estimate_ardl_multi(y, z, h = 1, p = c(1, 1))
coefs

}
